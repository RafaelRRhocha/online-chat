import { FC } from 'react';

interface NewTasksPageProps {}

const NewTasksPage: FC<NewTasksPageProps> = ({}) => {
  return (
    <div>
      <p>Página de new Task</p>
    </div>
  );
}

export default NewTasksPage;